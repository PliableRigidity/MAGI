# MAGI Command UI

This is a separate UI entrypoint that calls the existing backend API at `http://localhost:8000/api/decide`.

## Run

```powershell
cd magi_ui
npm install
npm run dev
```

The existing backend and the existing `frontend/` test UI remain untouched.
