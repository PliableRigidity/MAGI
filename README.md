# MAGI

Local MAGI-style deliberation engine with a FastAPI backend, Ollama-driven agent pipeline, and React dashboard.

## Run backend

```powershell
python main.py
```

Or directly:

```powershell
uvicorn backend.main:app --reload
```

## Run frontend

```powershell
cd frontend
npm install
npm run dev
```
